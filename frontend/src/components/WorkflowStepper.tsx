const stages=[['data_health','数据体检'],['governance','字段治理'],['variable_scan','变量扫描'],['candidate_rules','规则生成'],['stability','稳定性'],['rule_groups','规则聚类'],['grading','评级'],['report','报告']];
const label:any={NOT_STARTED:'未开始',RUNNING:'运行中',SUCCESS:'已完成',STALE:'需重算',FAILED:'失败'};
export default function WorkflowStepper({statuses,onRun}:{statuses:any;onRun:(stage:string,mode:'CURRENT'|'FROM_HERE')=>void}){return <div className="workflow"><div className="workflow-head"><b>Analysis Workflow</b><span>阶段可独立执行，也可从当前阶段继续。</span></div><div className="workflow-steps">{stages.map(([id,name],i)=><div className={'workflow-step '+String(statuses?.[id]||'NOT_STARTED').toLowerCase()} key={id}><div className="workflow-dot">{statuses?.[id]==='SUCCESS'?'✓':i+1}</div><div><b>{name}</b><small>{label[statuses?.[id]||'NOT_STARTED']}</small></div><button onClick={()=>onRun(id,'CURRENT')}>{statuses?.[id]==='SUCCESS'?'重跑':'运行'}</button><button onClick={()=>onRun(id,'FROM_HERE')}>从此处</button></div>)}</div></div>}
