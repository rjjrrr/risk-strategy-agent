import {useMemo,useState} from 'react'; import {Search} from 'lucide-react'; import {Tag,Metric,Empty} from '../components/Common'; import type {Governance} from '../types';
export default function GovernancePage({rows,onChange}:{rows:Governance[];onChange:(field:string,d:string)=>void}){const [q,setQ]=useState(''),[filter,setFilter]=useState('ALL'); const view=useMemo(()=>rows.filter(x=>(filter==='ALL'||x.decision===filter)&&x.field.toLowerCase().includes(q.toLowerCase())),[rows,q,filter]); if(!rows.length)return <div className="page"><Empty title="尚未执行字段治理" text="请先从数据导入页开始字段治理"/></div>; return <div className="page"><div className="page-title"><div><h2>字段治理</h2><p>语义、质量和可用性决策；人工 Override 会记录到当前数据集。</p></div></div><div className="metrics compact"><Metric label="Total Fields" value={rows.length}/><Metric label="KEEP" value={rows.filter(x=>x.decision==='KEEP').length}/><Metric label="Excluded" value={rows.filter(x=>x.decision==='EXCLUDE').length}/><Metric label="Suspect Leakage" value={rows.filter(x=>x.decision==='SUSPECT_LEAKAGE').length}/><Metric label="Identifier" value={rows.filter(x=>x.semantic_type==='IDENTIFIER').length}/></div><div className="toolbar"><span className="search"><Search size={15}/><input placeholder="搜索字段" value={q} onChange={e=>setQ(e.target.value)}/></span><select value={filter} onChange={e=>setFilter(e.target.value)}><option>ALL</option><option>KEEP</option><option>EXCLUDE</option><option>SUSPECT_LEAKAGE</option><option>SPECIAL</option></select></div><div className="table-wrap"><table><thead><tr><th>Field</th><th>Semantic Type</th><th>Detected Type</th><th>Valid</th><th>Missing</th><th>Unique</th><th>Decision</th><th>Reason</th><th>Action</th></tr></thead><tbody>{view.slice(0,500).map(g=><tr key={g.field}><td><b>{g.field}</b>{g.unique_ratio>.95&&<small className="risk-note">HIGH UNIQUE</small>}</td><td>{g.semantic_type}</td><td>{g.detected_type}</td><td>{(g.valid_rate*100).toFixed(1)}%</td><td>{(g.missing_rate*100).toFixed(1)}%</td><td>{(g.unique_ratio*100).toFixed(1)}%</td><td><Tag>{g.decision}</Tag></td><td className="reason">{g.reason}</td><td><select value={g.decision} onChange={e=>onChange(g.field,e.target.value)}><option>KEEP</option><option>EXCLUDE</option><option>SUSPECT_LEAKAGE</option><option>IDENTIFIER</option><option>REVIEW</option></select></td></tr>)}</tbody></table></div></div>}
