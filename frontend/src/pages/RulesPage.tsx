import {useEffect,useMemo,useState} from 'react';
import {Metric,Tag,Empty} from '../components/Common';
import Chart from '../components/Chart';
import {groups,groupMatrix} from '../api/client';
import type {Rule} from '../types';
const colors:any={STRONG:'#16855b',WEAK:'#b7791f',FAILED:'#c0392b',NOT_AVAILABLE:'#64748b'};
export default function RulesPage({rows,onSelect,datasetId}:{rows:Rule[];onSelect:(r:Rule)=>void;datasetId?:string}){
 const [seg,setSeg]=useState('ALL'),[activeSeg,setActiveSeg]=useState('NEW'),[grade,setGrade]=useState('ALL'),[q,setQ]=useState(''),[rep,setRep]=useState(true),[low,setLow]=useState(false),[ootFailed,setOotFailed]=useState(false),[view,setView]=useState('table'),[groupsData,setGroupsData]=useState<any[]>([]),[groupSummary,setGroupSummary]=useState<any>(null),[matrix,setMatrix]=useState<any>(null),[threshold,setThreshold]=useState(.9);
 const filtered=useMemo(()=>rows.filter(r=>(seg==='ALL'||r.segment===seg)&&(grade==='ALL'||r.grade===grade)&&(!ootFailed||r.oot_status==='FAILED')&&(r.field+r.rule).toLowerCase().includes(q.toLowerCase())&&(rep?r.is_representative!==false:true)&&(low||!r.small_segment)),[rows,seg,grade,ootFailed,q,rep,low]);
 useEffect(()=>{if(datasetId)groups(datasetId).then(r=>{setGroupsData(r.data.groups||r.data);setGroupSummary(r.data.summary||null)}).catch(()=>{});},[datasetId,rows]);
 const clusterSeg=seg==='ALL'?activeSeg:seg;
 useEffect(()=>{if(datasetId&&view!=='table')groupMatrix(datasetId,clusterSeg,threshold).then(r=>setMatrix(r.data)).catch(()=>setMatrix(null));},[datasetId,view,clusterSeg,threshold,rows]);
 const heat=matrix?{tooltip:{},visualMap:{min:0,max:1,calculable:true,orient:'horizontal',left:'center',bottom:0},xAxis:{type:'category',data:matrix.rule_ids},yAxis:{type:'category',data:matrix.rule_ids},series:[{type:'heatmap',data:matrix.matrix.flatMap((line:number[],i:number)=>line.map((v:number,j:number)=>[j,i,v]))}]}:null;
 const network=matrix?{tooltip:{},series:[{type:'graph',layout:'force',roam:true,force:{repulsion:120},data:matrix.nodes.map((n:any)=>({...n,symbolSize:10+Math.round((n.coverage||0)*18)+(n.is_representative?6:0),itemStyle:{color:n.is_representative?'#315efb':'#94a3b8'}})),links:matrix.edges,label:{show:true,position:'right'},lineStyle:{opacity:.65}}]}:null;
 return <div className="page"><div className="page-title"><div><h2>规则中心</h2><p>代表规则、OOT 稳定性和 Rule Group 聚类统一查看。</p></div></div><div className="metrics compact"><Metric label="Representative" value={rows.filter(r=>r.is_representative!==false).length}/><Metric label="A" value={rows.filter(r=>r.grade==='A').length}/><Metric label="B" value={rows.filter(r=>r.grade==='B').length}/><Metric label="OOT FAILED" value={rows.filter(r=>r.oot_status==='FAILED').length}/></div><div className="toolbar"><select value={seg} onChange={e=>{setSeg(e.target.value);setMatrix(null)}}><option>ALL</option><option>NEW</option><option>OLD</option></select><select value={grade} onChange={e=>setGrade(e.target.value)}><option>ALL</option><option>A</option><option>B</option><option>REVIEW</option><option>C</option></select><input placeholder="字段/规则搜索" value={q} onChange={e=>setQ(e.target.value)}/><label className="switch"><input type="checkbox" checked={ootFailed} onChange={e=>setOotFailed(e.target.checked)}/>只看 OOT FAILED</label><label className="switch"><input type="checkbox" checked={rep} onChange={e=>setRep(e.target.checked)}/>仅代表规则</label><label className="switch"><input type="checkbox" checked={low} onChange={e=>setLow(e.target.checked)}/>显示低样本</label><button onClick={()=>setView('table')}>规则表</button><button onClick={()=>setView('cluster')}>聚类网络</button><button onClick={()=>setView('matrix')}>Jaccard 矩阵</button></div>{view!=='table'&&<div className="toolbar"><b>聚类 Segment：</b><button className={clusterSeg==='NEW'?'active':''} onClick={()=>{setActiveSeg('NEW');setMatrix(null)}}>NEW</button><button className={clusterSeg==='OLD'?'active':''} onClick={()=>{setActiveSeg('OLD');setMatrix(null)}}>OLD</button><span>Same Group Threshold: 0.90</span><span>Display Threshold: {threshold.toFixed(2)}</span><input type="range" min=".8" max="1" step=".01" value={threshold} onChange={e=>setThreshold(Number(e.target.value))}/></div>}{view==='table'?<div className="table-wrap">{filtered.length?<table><thead><tr><th>Grade</th><th>Segment</th><th>Field</th><th>Rule</th><th>Hit / Bad</th><th>Coverage</th><th>Lift</th><th>Sampling</th><th>OOT</th><th>OOT Lift</th><th>Rule Group</th></tr></thead><tbody>{filtered.map((r,i)=><tr key={i} onClick={()=>onSelect(r)} className="clickable"><td><Tag>{r.grade}</Tag></td><td>{r.segment}</td><td>{r.field}</td><td>{r.rule}</td><td>{r.hit_count} / {r.bad_count}</td><td>{(r.coverage*100).toFixed(2)}%</td><td>{r.lift?.toFixed(2)}</td><td>{(r.bootstrap_positive_ratio*100).toFixed(1)}%</td><td><span style={{color:colors[r.oot_status||'NOT_AVAILABLE'],fontWeight:700}}>{r.oot_status||'NOT_AVAILABLE'}</span></td><td>{r.oot_lift?.toFixed(2)||'—'}</td><td>{r.rule_group_id}</td></tr>)}</tbody></table>:<Empty title="当前筛选条件下暂无候选规则" text="可以关闭仅代表规则或显示低样本规则。"/>}</div>:<div className="card chart-card"><div className="section-header"><h3>{view==='cluster'?'Rule Group 网络':'Jaccard 相似度矩阵'}</h3></div>{matrix?(view==='cluster'?<Chart option={network}/>:<Chart option={heat}/>):<Empty title="暂无矩阵" text="请先完成 Candidate Rule / Stability / Rule Group 阶段。"/>}</div>}{view==='cluster'&&groupsData.length>0&&<div className="table-wrap"><table><thead><tr><th>Group</th><th>Segment</th><th>Rules</th><th>Representative</th><th>OOT</th></tr></thead><tbody>{groupsData.filter(g=>g.representative?.segment===clusterSeg).map(g=><tr key={g.rule_group_id}><td>{g.rule_group_id}</td><td>{g.representative?.segment}</td><td>{g.count}</td><td>{g.representative?.rule}</td><td>{g.representative?.oot_status||'NOT_AVAILABLE'}</td></tr>)}</tbody></table></div>}</div>
}
