import Chart from '../components/Chart'; import {Metric,Tag,Empty} from '../components/Common';
export default function MiningPage({data,rules,onRun}:{data:any;rules:any[];onRun:()=>void}){if(!data)return <div className="page"><Empty title="尚未开始策略挖掘" text="完成字段治理后开始分析"/><button onClick={onRun}>开始策略分析</button></div>; const grade={NEW:data.rules.grades.NEW,OLD:data.rules.grades.OLD}; const option={legend:{bottom:0},xAxis:{type:'category',data:['NEW','OLD']},yAxis:{type:'value'},series:['A','B','REVIEW','C'].map((g:any)=>({name:g,type:'bar',stack:'total',data:[grade.NEW?.[g]||0,grade.OLD?.[g]||0]}))}; return <div className="page"><div className="page-title"><div><h2>策略挖掘</h2><p>仅展示单变量候选规则；Sampling Stability 不等价于跨月份 OOT 稳定性。</p></div><button onClick={onRun}>开始策略分析</button></div><div className="metrics"><Metric label="Candidate Rules" value={data.rules.total}/><Metric label="A Rules" value={(data.rules.grades.NEW?.A||0)+(data.rules.grades.OLD?.A||0)} tone="tone-a"/><Metric label="B Rules" value={(data.rules.grades.NEW?.B||0)+(data.rules.grades.OLD?.B||0)}/><Metric label="Review" value={(data.rules.grades.NEW?.REVIEW||0)+(data.rules.grades.OLD?.REVIEW||0)} tone="tone-review"/><Metric label="Rule Groups" value={data.rule_groups.count}/></div><div className="grid-2"><div className="card chart-card"><h3>规则等级分布</h3><Chart option={option}/></div><div className="card"><h3>NEW / OLD 规则摘要</h3><div className="segment-summary"><div><b>NEW</b><span>样本 {data.segments.NEW?.count||0}</span><strong>{((data.segments.NEW?.bad_rate||0)*100).toFixed(2)}%</strong><small>Bad Rate</small></div><div><b>OLD</b><span>样本 {data.segments.OLD?.count||0}</span><strong>{((data.segments.OLD?.bad_rate||0)*100).toFixed(2)}%</strong><small>Bad Rate</small></div></div></div></div><div className="card"><h3>当前 Representative Rules</h3>{rules.length?<table><thead><tr><th>Grade</th><th>Segment</th><th>Field</th><th>Rule</th><th>Lift</th><th>Group</th></tr></thead><tbody>{rules.filter(r=>r.is_representative!==false).slice(0,10).map((r,i)=><tr key={i}><td><Tag>{r.grade}</Tag></td><td>{r.segment}</td><td>{r.field}</td><td>{r.rule}</td><td>{r.lift?.toFixed(2)}</td><td>{r.rule_group_id}</td></tr>)}</tbody></table>:<Empty title="暂无候选规则" text="当前数据或筛选条件下没有正式候选规则"/>}</div></div>}
