import {Download,FileText} from 'lucide-react'; import {exportUrl} from '../api/client';
export default function ReportPage({datasetId}:{datasetId:string}){return <div className="page"><div className="page-title"><div><h2>分析报告</h2><p>下载正式结果文件，供策略 Review 和后续归档使用。</p></div></div><div className="export-grid">{[['rules','candidate_rules.csv','候选规则'],['governance','variable_governance.csv','字段治理'],['report','rule_report.md','Markdown 报告']].map(([key,name,label])=><a className="export-card" href={datasetId?exportUrl(datasetId,key):'#'} key={key}><FileText size={24}/><b>{label}</b><span>{name}</span><Download size={16}/></a>)}</div></div>}
